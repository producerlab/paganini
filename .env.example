# ===========================================
# Paganini Bot - Environment Configuration
# ===========================================
# Copy this file to .env and fill in your values
# cp .env.example .env

# ----- Telegram Bot -----
# Get token from @BotFather
TOKEN=your_telegram_bot_token_here

# Bot username (without @)
BOT_USERNAME=your_bot_username

# Admin Telegram IDs (comma-separated)
# Example: 123456789,987654321
ADMIN_IDS=123456789

# ----- Channel/Club Settings -----
# Channel for subscription check (with @)
CHANNEL_USERNAME=@your_channel

# Club chat ID for bonus checking (negative number for groups/channels)
CLUB_CHAT_ID=-1001234567890

# ----- Database -----
# PostgreSQL connection string (Railway provides this automatically)
# Format: postgresql+asyncpg://user:password@host:port/database
DB_URL=postgresql+asyncpg://postgres:password@localhost:5432/paganini

# Set to false in production
DB_ECHO=false

# ----- Paths -----
# Path to media folder (screenshots, etc.)
MEDIA_ROOT=./media

# Path for saving generated reports
DATA_ROOT=./data

# ----- Модуль Банк Payment -----
# Get credentials from Modulbank personal cabinet
MODULBANK_MERCHANT_ID=your_merchant_id
MODULBANK_SECRET_KEY=your_production_secret_key
MODULBANK_TEST_SECRET_KEY=your_test_secret_key

# Test mode: 1 = test payments, 0 = real payments
MODULBANK_TEST_MODE=1

# Webhook URL for payment notifications (your public server URL)
# Example: https://your-app.railway.app/webhook/modulbank
MODULBANK_WEBHOOK_URL=https://your-server.com/webhook/modulbank

# ----- Webhook Server -----
# Host and port for the webhook server
WEBHOOK_HOST=0.0.0.0
WEBHOOK_PORT=8080

# ----- Security -----
# AES-256 encryption key for WB tokens (base64 encoded, 32 bytes)
# Generate with: python -c "from services.crypto import generate_encryption_key; print(generate_encryption_key())"
ENCRYPTION_KEY=
